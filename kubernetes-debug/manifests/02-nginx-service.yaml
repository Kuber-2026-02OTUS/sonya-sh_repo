apiVersion: v1
kind: Service
metadata:
  name: nginx-distroless
  namespace: kubernetes-debug
spec:
  type: ClusterIP
  selector:
    app: nginx-distroless
  ports:
    - name: http
      port: 80
      targetPort: http
